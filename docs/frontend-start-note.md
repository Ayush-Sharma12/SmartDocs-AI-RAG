Frontend start helper

- If you see CORS or dev server issues, run `npm ci` then `npm run dev` inside `/frontend`.
- On Windows, use PowerShell `./start-frontend.ps1` if you prefer the included script.
